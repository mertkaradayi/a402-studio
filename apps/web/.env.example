# ===========================================
# a402 Playground - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# IMPORTANT: Restart the dev server after changing these values!

# -----------------------------------------
# Sui Network Configuration
# -----------------------------------------
# Set to "testnet" or "mainnet"
# This controls:
#   - Wallet connection network
#   - Network indicator in UI
#   - Receipt chain field (sui-testnet or sui-mainnet)
NEXT_PUBLIC_SUI_NETWORK=testnet

# -----------------------------------------
# Beep SDK Configuration
# -----------------------------------------
# Get your publishable key from https://app.justbeep.it (Account Settings)
# This is for the Checkout Widget - safe to expose in frontend
NEXT_PUBLIC_BEEP_PUBLISHABLE_KEY=your-beep-publishable-key

# Beep API server URL (optional, defaults to production)
NEXT_PUBLIC_BEEP_SERVER_URL=https://api.justbeep.it

# -----------------------------------------
# Signature Verification (Optional)
# -----------------------------------------
# NOT NEEDED for most use cases - Beep verifies receipts internally
# Only needed for local Ed25519 verification without Beep API
# NEXT_PUBLIC_BEEP_FACILITATOR_PUBKEY=0x...

# -----------------------------------------
# API Server Configuration
# -----------------------------------------
# Backend API URL for proxy and a402 routes
NEXT_PUBLIC_API_URL=http://localhost:3001

