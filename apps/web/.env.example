NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_SUI_NETWORK=testnet

# Beep Authentication
# Used for client-side widgets (optional if only using MCP via backend)
NEXT_PUBLIC_BEEP_PUBLISHABLE_KEY=beep_pk_example_key
