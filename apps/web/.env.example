# ===========================================
# a402 Studio (Web App) - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# IMPORTANT: Restart the dev server after changing these values!
#
# LOCAL DEVELOPMENT:
#   cp .env.example .env.local
#   # Edit .env.local with your values
#   yarn dev
#
# PRODUCTION (Vercel):
#   Set these variables in Vercel dashboard > Settings > Environment Variables

# -----------------------------------------
# Sui Network Configuration
# -----------------------------------------
# Set to "testnet" or "mainnet"
# This controls:
#   - Wallet connection network
#   - Network indicator in UI
#   - Receipt chain field (sui-testnet or sui-mainnet)
NEXT_PUBLIC_SUI_NETWORK=mainnet

# -----------------------------------------
# Beep SDK Configuration (REQUIRED)
# -----------------------------------------
# Get your publishable key from https://app.justbeep.it (Account Settings > API Keys)
# This is for the Checkout Widget - safe to expose in frontend
# Format: beep_pk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
NEXT_PUBLIC_BEEP_PUBLISHABLE_KEY=beep_pk_your_publishable_key_here

# Beep API server URL (optional, defaults to production)
NEXT_PUBLIC_BEEP_SERVER_URL=https://api.justbeep.it

# -----------------------------------------
# API Server Configuration (REQUIRED for verification)
# -----------------------------------------
# Backend API URL for verification endpoints
#
# LOCAL DEVELOPMENT:
#   NEXT_PUBLIC_API_URL=http://localhost:3001
#
# PRODUCTION (Vercel):
#   Set this to your Railway API URL, e.g.:
#   NEXT_PUBLIC_API_URL=https://your-api-production.up.railway.app
#
# IMPORTANT: This MUST be set in production for payment verification to work!
# The API provides:
#   - /a402/verify-beep - Server-side Beep invoice verification
#   - /a402/verify-onchain - On-chain transaction verification
NEXT_PUBLIC_API_URL=http://localhost:3001

# -----------------------------------------
# Optional: Signature Verification
# -----------------------------------------
# NOT NEEDED for most use cases - the API handles verification
# Only needed for local Ed25519 verification without the backend API
# NEXT_PUBLIC_BEEP_FACILITATOR_PUBKEY=0x...
